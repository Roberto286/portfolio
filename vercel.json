{
	"build": {
		"env": {
			"HUGO_VERSION": "0.157.0",
			"GO_VERSION": "1.22"
		}
	},
	"installCommand": "cd site && hugo mod tidy && hugo mod npm pack && npm install",
	"buildCommand": "cd site && hugo --gc --minify --cleanDestinationDir",
	"outputDirectory": "site/public"
}
